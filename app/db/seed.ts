// TODO: import { getDb } from "../api/queries/connection" and tables from "./schema"

async function seed() {
  console.log("Seeding database...");

  // TODO: insert seed data, e.g.
  // const db = getDb();
  // await db.insert(schema.rainPatterns).values([ ... ]);

  console.log("Done.");
  process.exit(0);
}

seed();
